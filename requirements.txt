# Core dependencies for Security Analyzer
# Install with: pip install -r requirements.txt

# JavaScript/TypeScript parsing
esprima>=4.0.1

# PHP parsing
phply>=1.2.6

# PDF generation (optional but recommended)
reportlab>=4.0.0
matplotlib>=3.5.0

# Alternative: Use pip install -r requirements.txt
